syntax = "proto3";

package net; // 패키지 이름을 설정합니다.

message SockStatInfo {
    uint64 use_count = 1;
    uint64 in_use = 2;
    uint64 orphan = 3;
    uint64 tw = 4;
    uint64 alloc = 5;
    double mem_kb = 6;
}